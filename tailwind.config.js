/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './*.html', // All HTML files in the root
    './*.js'    // All JS files in the root (if needed)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}


